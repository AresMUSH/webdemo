<div class="row">
 <div class="col col-md-12">
      
     <h1>{{model.title}}</h1>
     
     <ul class="list-group">      
        {{#each model.posters as |poster| }}
          {{forum-response from=poster message="Some message."}}
        {{/each}}
     </ul>
     
  </div>
</div>

{{outlet}}
